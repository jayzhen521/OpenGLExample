add_library(gl3w
    STATIC
    ${GL3W_INCLUDE_DIR}/GL/gl3w.h
    #gl3w.c was generated by project gl3w(https://github.com/skaslev/gl3w)
    gl3w.c
)

target_include_directories(
    gl3w
    PRIVATE
    ${GL3W_INCLUDE_DIR}
)

set_target_properties(gl3w PROPERTIES LINKER_LANGUAGE C)